return {
    name        = "merge-compile-commands",
    description = "Merge all compile_commands/*.json into a single compile_commands.json",
    version     = "1.0",
    author      = "Your Name",
    requires    = { "premake" }
}
